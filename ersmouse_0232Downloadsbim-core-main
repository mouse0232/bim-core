[1mdiff --git a/src/clients/base.rs b/src/clients/base.rs[m
[1mindex 10b4888..18d1746 100644[m
[1m--- a/src/clients/base.rs[m
[1m+++ b/src/clients/base.rs[m
[36m@@ -65,37 +65,44 @@[m [mpub fn make_connection(address: &SocketAddr, url: &Url) -> Result<Box<dyn Generi[m
     let ssl = if url.scheme() == "https" { true } else { false };[m
     let mut retry = 3;[m
 [m
[31m-    let mut root_store = RootCertStore::empty();[m
[31m-    root_store.extend(webpki_roots::TLS_SERVER_ROOTS.iter().cloned());[m
[32m+[m[32m    while retry > 0 {[m
[32m+[m[32m        match TcpStream::connect_timeout(&address, Duration::from_secs(5)) {[m
[32m+[m[32m            Ok(stream) => {[m
[32m+[m[32m                #[cfg(debug_assertions)][m
[32m+[m[32m                debug!("TCP connected");[m
 [m
[31m-    let config = rustls::ClientConfig::builder()[m
[31m-        .with_root_certificates(root_store)[m
[31m-        .with_no_client_auth();[m
[32m+[m[32m                let _r = stream.set_write_timeout(Some(Duration::from_secs(30)));[m
[32m+[m[32m                let _r = stream.set_read_timeout(Some(Duration::from_secs(30)));[m
[32m+[m[32m                if !ssl {[m
[32m+[m[32m                    return Ok(Box::new(stream));[m
[32m+[m[32m                }[m
 [m
[31m-    let host_str = url.host_str().unwrap();[m
[31m-    let server_name = ServerName::try_from(host_str.to_string()).unwrap();[m
[31m-    let conn = rustls::ClientConnection::new(Arc::new(config), server_name).unwrap();[m
[32m+[m[32m                let mut root_store = RootCertStore::empty();[m
[32m+[m[32m                root_store.extend(webpki_roots::TLS_SERVER_ROOTS.iter().cloned());[m
 [m
[31m-    while retry > 0 {[m
[31m-        if let Ok(stream) = TcpStream::connect_timeout(&address, Duration::from_micros(1_000_000)) {[m
[31m-            #[cfg(debug_assertions)][m
[31m-            debug!("TCP connected");[m
[32m+[m[32m                let config = rustls::ClientConfig::builder()[m
[32m+[m[32m                    .with_root_certificates(root_store)[m
[32m+[m[32m                    .with_no_client_auth();[m
 [m
[31m-            let _r = stream.set_write_timeout(Some(Duration::from_secs(3)));[m
[31m-            let _r = stream.set_read_timeout(Some(Duration::from_secs(3)));[m
[31m-            if !ssl {[m
[31m-                return Ok(Box::new(stream));[m
[31m-            }[m
[32m+[m[32m                let host_str = url.host_str().unwrap();[m
[32m+[m[32m                let server_name = ServerName::try_from(host_str.to_string()).unwrap();[m
[32m+[m[32m                let conn = rustls::ClientConnection::new(Arc::new(config), server_name).unwrap();[m
 [m
[31m-            let tls = rustls::StreamOwned::new(conn, stream);[m
[32m+[m[32m                let tls = rustls::StreamOwned::new(conn, stream);[m
 [m
[31m-            #[cfg(debug_assertions)][m
[31m-            debug!("SSL connected");[m
[32m+[m[32m                #[cfg(debug_assertions)][m
[32m+[m[32m                debug!("SSL connected");[m
 [m
[31m-            return Ok(Box::new(tls));[m
[32m+[m[32m                return Ok(Box::new(tls));[m
[32m+[m[32m            }[m
[32m+[m[32m            Err(e) => {[m
[32m+[m[32m                #[cfg(debug_assertions)][m
[32m+[m[32m                debug!("Connection attempt failed: {}", e);[m
[32m+[m[32m            }[m
         }[m
 [m
         retry -= 1;[m
[32m+[m[32m        thread::sleep(Duration::from_millis(100));[m
     }[m
     [m
     Err(String::from("连接失败"))[m
[36m@@ -169,42 +176,15 @@[m [mimpl LoadCounter {[m
             return 0.0;[m
         }[m
         [m
[31m-        // 添加异常值过滤机制[m
[31m-        let mut speeds = Vec::new();[m
[31m-        [m
[31m-        // 计算多个时间窗口的速度[m
[31m-        for i in 10..results.len() - 5 {[m
[31m-            let (c1, t1) = results[i-5];[m
[31m-            let (c2, t2) = results[i];[m
[31m-            [m
[31m-            if t2 > t1 && c2 >= c1 {[m
[31m-                let speed = ((c2 - c1) * 8) as f64 / (t2 - t1) as f64;[m
[31m-                speeds.push(speed);[m
[31m-            }[m
[31m-        }[m
[31m-        [m
[31m-        if speeds.is_empty() {[m
[31m-            return 0.0;[m
[31m-        }[m
[31m-        [m
[31m-        // 对速度进行排序并去除异常值[m
[31m-        speeds.sort_by(|a, b| a.partial_cmp(b).unwrap());[m
[31m-        [m
[31m-        // 去除前5%和后5%的极值[m
[31m-        let remove_count = (speeds.len() as f64 * 0.05).ceil() as usize;[m
[31m-        let start = remove_count.min(speeds.len());[m
[31m-        let end = speeds.len().saturating_sub(remove_count);[m
[31m-        [m
[31m-        if start >= end {[m
[31m-            return speeds[speeds.len() / 2]; // 返回中位数[m
[31m-        }[m
[31m-        [m
[31m-        // 计算剩余值的平均值[m
[31m-        let sum: f64 = speeds[start..end].iter().sum();[m
[31m-        sum / (end - start) as f64[m
[32m+[m[32m        let (c18, t18) = results[17];[m
[32m+[m[32m        let (c28, t28) = results[27];[m
[32m+[m
[32m+[m[32m        ((c28 - c18) * 8) as f64 / (t28 - t18) as f64[m
     }[m
 [m
     pub fn status(&self) -> String {[m
[32m+[m[32m        let mut stop = 0;[m
[32m+[m[32m        let mut last = 0;[m
         let results = self.results.lock().unwrap().to_vec();[m
 [m
         #[cfg(debug_assertions)][m
[36m@@ -215,20 +195,30 @@[m [mimpl LoadCounter {[m
             return String::from("无数据");[m
         }[m
 [m
[31m-        // 添加异常值过滤机制[m
[31m-        let mut stops = 0;[m
[31m-        let mut last = 0;[m
[32m+[m[32m        // 检查是否所有数据都已完成传输（最后几次计数相同表示传输完成）[m
[32m+[m[32m        let mut consecutive_same = 0;[m
[32m+[m[32m        for (num, _) in results.iter().rev().take(3) {[m
[32m+[m[32m            if *num == last || last == 0 {[m
[32m+[m[32m                consecutive_same += 1;[m
[32m+[m[32m            }[m
[32m+[m[32m            last = *num;[m
[32m+[m[32m        }[m
         [m
[31m-        // 检查数据变化情况，识别断流[m
[31m-        for (num, _) in results.iter().skip(5) { // 跳过前几个数据点[m
[32m+[m[32m        // 如果最后几次计数相同，说明传输已完成，不应该是断流[m
[32m+[m[32m        if consecutive_same >= 3 {[m
[32m+[m[32m            return String::from("正常");[m
[32m+[m[32m        }[m
[32m+[m
[32m+[m[32m        // 原始断流检测逻辑[m
[32m+[m[32m        last = 0;[m
[32m+[m[32m        for (num, _) in results.iter() {[m
             if *num == last {[m
[31m-                stops += 1;[m
[32m+[m[32m                stop += 1;[m
             }[m
             last = *num;[m
         }[m
 [m
[31m-        // 根据停止次数判断状态[m
[31m-        if stops < 3 {[m
[32m+[m[32m        if stop < 6 {[m
             String::from("正常")[m
         } else {[m
             String::from("断流")[m
